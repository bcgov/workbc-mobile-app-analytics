# Safe local defaults only — copy to `.env` and adjust; never commit secrets.

PORT=4000
NODE_ENV=development
LOG_LEVEL=info
API_KEY=change-me
DATABASE_URL=postgresql://analytics_user:your_password@localhost:5432/analytics
